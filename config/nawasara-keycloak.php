<?php

return [
    // Token cache TTL in seconds (Keycloak default token lifetime = 60s)
    'token_ttl' => 55,

    // Users per page when listing
    'users_per_page' => 20,

    // Events per page
    'events_per_page' => 25,
];
